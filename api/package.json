{
  "name": "api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create attendance-db",
    "db:migrate": "wrangler d1 execute attendance-db --file=./schema.sql",
    "db:query": "wrangler d1 execute attendance-db --command"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
